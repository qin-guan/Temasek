# Environment Configuration for Temasek Calendar Sync Worker
# Copy this file to .env and fill in your values

# Google Service Account Credentials (Base64 encoded JSON)
# Get this from Google Cloud Console -> Service Accounts -> Keys
CALENDAR_SERVICE_ACCOUNT_JSON=your_base64_encoded_service_account_json_here

# Primary Calendar ID (source calendar to sync FROM)
# This can be an email address like your-calendar@gmail.com
# Or a calendar ID from Google Calendar settings
PRIMARY_CALENDAR_ID=your-primary-calendar@gmail.com

# Secondary Calendar ID (target calendar to sync TO) 
# This can be an email address like backup-calendar@gmail.com
# Or a calendar ID from Google Calendar settings
SECONDARY_CALENDAR_ID=your-secondary-calendar@gmail.com

# Sync interval in milliseconds (optional, defaults to 60000 = 1 minute)
# Set to 30000 for 30 seconds, 300000 for 5 minutes, etc.
SYNC_INTERVAL_MS=60000

# Example of how to generate the service account JSON:
# 1. Go to https://console.cloud.google.com/
# 2. Create or select a project
# 3. Enable Google Calendar API
# 4. Go to "Credentials" -> "Create Credentials" -> "Service Account"
# 5. Download the JSON key file
# 6. Base64 encode it: base64 -w 0 path/to/service-account.json
# 7. Use that encoded string for CALENDAR_SERVICE_ACCOUNT_JSON

# Make sure to share both calendars with the service account email address!